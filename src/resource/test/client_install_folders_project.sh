#!/bin/bash
#The script written below is used to create a new directory
# . audio.sh;. image.sh;. video.sh;. web.sh;

# audio project
mkdir -p "audio project"/{archives/exports/{final,stems,unused},Project/{Samples/{Recorded,Processed/{Consolidate,Freeze,Reverse}},{Backup,"Ableton Project Info","Audio Files"}}}
# image project
mkdir -p "image project"/{archives,exports/{advertisement,banner,cover,icon},assets/{docs/{bizcards,contract},img/{icon,logo,unsorted}}}
# video project
mkdir -p "video project"/{archives,contracts,imports,exports/{advertisment,banner}}
# web project
mkdir -p "web project"/web/{pages/bookmarks,assets/{docs/{bizcards,contract},scripts,css/scss,img/{icon,logo,unsorted},video/{advertisment,banner,"music video",unsorted}}}
